# Secrets and environment - NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
*.crt
id_rsa*
id_ed25519*
kubeconfig*
*.tfvars
!*.tfvars.example

# Terraform state (contains sensitive data)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Credentials
secrets/
credentials/
*-credentials.json
service-account*.json

# Data and backups
*.sql
*.dump
backups/
*.tar.gz

# Dependencies and build
node_modules/
dist/
build/

# Logs and temp
*.log
logs/
tmp/
.DS_Store

# Internal notes (not public)
NOTES_INTERNAL*

# Ansible - inventory and host vars with real data
ansible/inventory.ini
ansible/group_vars/all.yml
