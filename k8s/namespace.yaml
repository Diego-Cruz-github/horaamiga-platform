# Namespace isolates HoraAmiga resources inside the cluster.
# Helps separate environments (e.g. horaamiga, horaamiga-staging) and apply quotas/policies.
apiVersion: v1
kind: Namespace
metadata:
  name: horaamiga
  labels:
    project: horaamiga
