# Copie para terraform.tfvars e preencha (terraform.tfvars NUNCA e versionado).
# Alternativa: exportar como variavel de ambiente, ex: export TF_VAR_hcloud_token=...

hcloud_token         = "<token-hetzner>"
cloudflare_api_token = "<token-cloudflare>"
cloudflare_zone_id   = "<zone-id-do-dominio>"

admin_ip = "<seu-ip-publico>" # ex: 203.0.113.10

# Opcionais (tem default em variables.tf):
# server_type = "cx22"
# server_location = "nbg1"
