# Handlers: tasks that only run when notified (e.g. the SSH config changed).
# Avoids pointless restarts - the service restarts only if the config actually changed.

- name: Restart SSH
  ansible.builtin.service:
    name: ssh
    state: restarted
