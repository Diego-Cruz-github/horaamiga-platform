# Copy to inventory.ini and fill in the real IP (from the Terraform output).
# inventory.ini is gitignored.

[web]
horaamiga-prod ansible_host=<SERVER_IP> ansible_user=deploy ansible_ssh_private_key_file=~/.ssh/id_ed25519
