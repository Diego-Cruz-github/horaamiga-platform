# Copie para inventory.ini e preencha com o IP real (sai do output do Terraform).
# inventory.ini fica no .gitignore.

[web]
horaamiga-prod ansible_host=<IP_DO_SERVIDOR> ansible_user=deploy ansible_ssh_private_key_file=~/.ssh/id_ed25519
